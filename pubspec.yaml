name: country_state_city
description: A flutter package to display list of Countries, States and Cities depends on a given country
version: 0.2.0

homepage: https://github.com/paulpascal/country_state_city
repository: https://github.com/paulpascal/country_state_city

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  assets:
    - lib/assets/
